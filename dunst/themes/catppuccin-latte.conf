;-------------------------
; Catppuccin Latte — dunst palette (colors only). The one light flavor.
;
; Dropped into ~/.config/dunst/dunstrc.d/catppuccin.conf by ~/setup/apply-theme.sh.
; dunst loads dunstrc first, then dunstrc.d/*.conf (which override it), so these
; colors win over the hand-maintained layout in dunstrc. Same split as polybar
; (config.ini = layout, colors.ini = palette). Accent = mauve, matching i3 /
; polybar / GTK / yazi.
;--------------------------

[global]
    frame_color    = "#8839ef"
    separator_color = frame

[urgency_low]
    background  = "#eff1f5"
    foreground  = "#6c6f85"
    frame_color = "#bcc0cc"

[urgency_normal]
    background  = "#eff1f5"
    foreground  = "#4c4f69"
    frame_color = "#8839ef"

[urgency_critical]
    background  = "#eff1f5"
    foreground  = "#4c4f69"
    frame_color = "#d20f39"
